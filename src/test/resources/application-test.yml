spring:
  application:
    name: chat-app

  datasource:
    url: jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: long-256-bit-secret-key-for-testing-purposes-only
  expiration: 86400000

#spring:
#  application:
#    name: chat-app
#
#  datasource:
#    url: jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
#    username: ${DB_USERNAME:we}
#    password: ${DB_PASSWORD:we}
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    show-sql: false
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.H2Dialect
#        format_sql: true
#
#  flyway:
#    enabled: true
#
#
#jwt:
#  secret: ${JWT_SECRET:long-256-bit-secret-key}
#  expiration: 86400000
